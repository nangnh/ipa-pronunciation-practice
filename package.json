{
  "name": "copy-of-ipa-pronunciation-practice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -R locales dist/locales && cp -R icons dist/icons",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
